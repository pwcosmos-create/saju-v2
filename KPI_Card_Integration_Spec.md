# 📊 KPI 카드 통합 시스템 동작 사양서 (Final Spec)
## 목표 및 원칙 [근거: Designer 검증된 지식]
*   **목표:** Deep Copper/Clear Sky Blue 디자인 원칙을 기반으로, `KPI_Card.jsx`가 대시보드 Shell 내 모든 데이터 상태(정상, 경고, 에러)에서 일관성과 권위성을 유지하도록 동작 정의.
*   **핵심 원칙:** **데이터 흐름의 시각적 연속성 (Clear Sky Blue)** > **핵심 수치의 강조 (Deep Copper)** > **기본 레이아웃/배경 (Midnight Blue)**

## 1. 컴포넌트 구조 및 반응형 사양 [근거: Designer 검증된 지식]
*   **레이아웃:** Grid System 기반의 4열 그리드 시스템을 기본으로 하며, 모바일 환경에서는 1열 스태킹(Stacking) 방식을 적용한다.
    *   `Desktop (>=1280px)`: 4개 컴포넌트 x 3줄 배치 권장. 간격: `16px`.
    *   `Tablet (768px - 1280px)`: 2개 컴포넌트씩 묶어 그리드 처리.
    *   `Mobile (<768px)`: 수직 스택(Vertical Stack). 각 KPI 카드는 전체 너비(`100%`)를 사용한다.

## 2. 데이터 상태별 시각화 명세 (State Management Visualization)
| State | 발생 상황 | Deep Copper (`#B8860B`) 적용 영역 | Clear Sky Blue (`#ADD8E6`) 적용 영역 | Midnight Blue (`#0A1931`) 역할 | 추가 동작 및 가이드라인 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **✅ Normal (정상)** | 실시간 데이터가 안정적으로 수신됨. | KPI 핵심 수치(Value), CTA 버튼. | 추이 그래프 연결선, Change Rate 화살표. | 배경 및 레이블 텍스트. | 변화율(`%`)은 반드시 작은 폰트 크기로 명시하여 권위성을 유지한다. |
| **⚠️ Warning (경고)** | 데이터가 임계값(Threshold)에 도달했거나, 전일 대비 급격한 변동(>15%) 발생. | 경고 메시지 배경 오버레이 또는 수치 주변 강조. | 연결선 색상을 `Warning Yellow`로 일시 변경 (Accent 확장). | 섹션 구분 및 타이틀 유지. | **반드시 경고 아이콘(!)**을 사용하고, 툴팁(`Tooltip`)에 구체적인 임계값 기준을 명시해야 한다. |
| **❌ Error/Missing** | API 호출 실패, 데이터 없음(Null/Undefined), 또는 로딩 중 (Pending). | *Deep Copper 미사용.* 핵심 수치 영역 전체를 흐리게 처리(Blur 효과)한다. | 연결선 표시 대신 `[DATA ERROR]` 메시지 박스 사용. | 배경색 유지. | **가장 중요:** 에러 발생 시, 단순히 숫자를 비우는 것이 아니라 "데이터 로드 실패: [API 이름]" 형태로 명시적 에러 출처를 제공해야 한다. (시스템 통제력 강조) |
| **⏳ Loading** | 데이터 수신 대기 중. | *Deep Copper 미사용.* | 연결선 표시 대신 애니메이션화된 점(Dot) 또는 스켈레톤 로더(`Skeleton Loader`) 사용. | 배경색 유지. | 스켈레톤 로더는 실제 컴포넌트의 구조를 모방하여 '시스템이 작동하고 있다'는 인상을 주어야 한다. |

## 3. 데이터 흐름 및 상호작용 원칙
1.  **데이터 흐름 강조 (Clear Sky Blue):** 모든 KPI 카드는 마치 하나의 시스템에서 유기적으로 연결된 것처럼 보이게 하여, 각 수치가 독립적이지 않음을 시각적으로 증명해야 한다. (예: 4개 카드 중 A의 데이터 변화가 B와 C의 추이 그래프에 연관되어 표시됨).
2.  **Deep Copper 우선순위:** KPI 카드의 핵심 지표(KPI)는 다른 어떤 요소보다도 Deep Copper를 통해 시청자의 눈을 먼저 사로잡도록 설계한다. (정보 계층 구조 최우선화)
3.  **오류 처리의 시스템화:** 오류 상태(`❌`)를 단순히 실패가 아닌, '시스템이 감지한 이상 징후'로 프레이밍하여 보여줌으로써, 우리의 서비스가 **데이터 흐름을 완벽하게 모니터링하는 통제된 시스템**임을 강조한다.

---
*본 사양서는 기존의 디자인 원칙(Deep Copper/Clear Sky Blue)을 유지하되, 실시간 데이터 환경에서 필요한 '운영적 견고성'과 '시스템 투명성'이라는 측면을 추가하여 확장한 통합 가이드라인입니다.*