# 🎨 Figma 애니메이션 구현 흐름 지도 (Flow Map) V2.0 - 기술 사양 기반 최종 명세서

## 목표
이 Flow Map은 `Final_Design_System_Spec_V1.0.md`의 시각적 목표와 `Code_Implementation_Guide_V1.0.md`의 기술적 제약(State Machine, KPI 바인딩)을 통합하여, Figma 프로토타입 개발에 필요한 최종 애니메이션 구현 사양입니다.

## ⚡️ 핵심 원칙 재확인
1. **상태 기반 전환 (State-Driven):** 모든 시각 변화는 사용자 액션이나 데이터 상태(예: `Initial State` $\rightarrow$ `Data Input State` $\rightarrow$ `Success State`)에 의해 트리거되어야 합니다.
2. **색상 코드 적용:** Deep Navy (`#0A1931`), Deep Copper (`#B8860B`), Clear Sky Blue (`#ADD8E6`)는 애니메이션의 *강도*와 *가치*를 나타내는 데 사용됩니다.
3. **데이터 속도 연동:** KPI 값의 변화 폭과 중요도에 따라 애니메이션의 지속 시간(Duration)을 동적으로 조정해야 합니다.

---

## 🗺️ 섹션별 상세 애니메이션 구현 사양

### 1. 온보딩 / Initial State (불확실성 유도 단계)
*   **시각 목표:** 낮은 정보 노출, 미성숙함 강조 (Deep Navy 지배적).
*   **애니메이션 로직:**
    *   **트리거:** 페이지 로드 $\rightarrow$ 첫 번째 섹션 진입.
    *   **효과 1 (데이터 은폐):** 주요 KPI 수치는 초기에는 `...` 또는 흐릿한 배경(Deep Navy)으로 처리되어야 합니다. 데이터가 채워지기 시작할 때(진입 트리거), 부드럽게 페이드 인(`Fade In`) 됩니다.
    *   **효과 2 (정보 제한):** '핵심 기능' 설명은 한 번에 모두 보여주지 않고, 스크롤을 내릴 때마다 마치 **시스템이 단계적으로 정보를 해제하는 듯한** `Reveal` 효과(좌/우 슬라이드 또는 페이드)를 적용합니다.
    *   **속도:** 느리고 의도적인 속도 (Duration: 800ms 이상).

### 2. 데이터 입력 / Control Struggle State (불안감 극대화 단계)
*   **시각 목표:** 사용자의 불안감을 시각적 위협과 정보 과부하로 표현합니다.
*   **애니메이션 로직:**
    *   **트리거:** 사용자가 잘못된 데이터를 입력하거나, KPI가 기대치보다 낮을 때 (데이터 바인딩 실패).
    *   **효과 1 (위험 경고):** 오류 메시지는 단순한 빨간색이 아닌, Deep Navy 배경 위에 명확하게 대비되는 **경고성 깜빡임(Pulsing)** 애니메이션(`Animation: Pulse`)을 사용합니다.
    *   **효과 2 (데이터 흐름 차단):** 정상적인 데이터 연결선(Clear Sky Blue) 대신, 일시적으로 끊기거나 불안정하게 떨리는 (`Jitter/Wobble`) 애니메이션을 적용하여 시스템적 위협감을 극대화합니다.
    *   **속도:** 순간적이고 급박한 속도 (Duration: 300ms 이내).

### 3. 성공 상태 / Control Achieved State (통제권 확보 단계) - **가장 중요**
*   **시각 목표:** 압도적인 성취감과 시스템의 절대적 안정성을 경험하게 합니다.
*   **애니메이션 로직:**
    *   **트리거:** KPI 수치가 임계점(Threshold)을 돌파하거나, 구독 결제가 성공적으로 완료되었을 때.
    *   **효과 1 (Golden Flash):** 핵심 KPI 수치와 CTA 버튼은 **Deep Copper색의 강력한 플래시 효과(`Flash/Scale Up`)**를 동반하여 나타나야 합니다. 이 빛은 다른 모든 요소를 압도합니다.
    *   **효과 2 (데이터 폭발):** 성공 데이터가 표시될 때, 단순 증가 그래프가 아닌, 마치 **시스템의 에너지가 폭발하듯** Clear Sky Blue 라인들이 화면을 가로지르며 상승하는(`Burst Line`) 애니메이션을 적용합니다.
    *   **효과 3 (최종 결론):** 최종적으로 '통제권 확보' 메시지는 전체 화면에 걸쳐 부드럽게 확대되거나, 금빛 빛줄기가 배경을 감싸는 (`Glow Overlay`) 효과로 마무리됩니다.
    *   **속도:** 극도로 빠르고 강력한 속도 (Duration: 500ms - 1s).

---

## ✅ 개발팀 검증 체크리스트 (개발 전 필수 확인)
| 항목 | 구현 사양 | 기술적 연결점 (Code/Figma) | 비고 |
| :--- | :--- | :--- | :--- |
| **KPI 바인딩** | Deep Copper 색상으로, 300ms 간격의 스케일업 애니메이션 적용. | `[DPSR]` 검증 필수. 데이터 수치 변화에 직접 연동. | 핵심 증거물. 가장 높은 우선순위. |
| **상태 전이** | 각 상태(Initial $\rightarrow$ Struggle $\rightarrow$ Success)별로 애니메이션 키프레임 및 지속 시간을 분리 정의. | Redux/Zustand State Machine과 동기화되어야 함. | `Subscription_Funnel_Visual_Flow_Spec_V1.0.md` 참조. |
| **트랜지션** | 섹션 간 이동 시, Deep Navy 배경의 미묘한 질감 변화(Gradient Shift)를 통해 시스템적 연속성을 유지. | 단순 페이드 아웃/인 금지. 반드시 `[Transition Effect]` 명시. | 몰입도 유지에 중요. |